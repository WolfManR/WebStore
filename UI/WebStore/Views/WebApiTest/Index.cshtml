@model IEnumerable<string>
@{
    ViewBag.Title = "Test WebApi";
}

<div class="container">
    <ul>
        @foreach (var value in Model)
        {
            <li>@value</li>
        }
    </ul>
</div>