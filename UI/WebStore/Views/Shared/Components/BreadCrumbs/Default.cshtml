@model BreadCrumbViewModel

<div class="breadcrumbs">
    <ol class="breadcrumb">
        <li><a asp-controller="Home" asp-action="Index">Home</a></li>
        <li class="active"><a asp-controller="Shop" asp-action="Products">Catalog</a></li>
        @if (Model.Section != null)
        {
            if (Model.Section.ParentSection != null)
            {
                <li class="active">
                    <a asp-controller="Shop" asp-action="Products" asp-route-SectionId="@Model.Section.ParentSection.Id">
                        @Model.Section.ParentSection.Name
                    </a>
                </li>

            }

            <li class="active">
                <a asp-controller="Shop" asp-action="Products" asp-route-SectionId="@Model.Section.Id">
                    @Model.Section.Name
                </a>
            </li>
        }
        @if (Model.Brand != null)
        {
            <li class="active">
                <a asp-controller="Shop" asp-action="Products" asp-route-BrandId="@Model.Brand.Id">
                    @Model.Brand.Name
                </a>
            </li>
        }
        @if (Model.Product != null)
        {
            <li>@Model.Product</li>
        }
    </ol>
</div>